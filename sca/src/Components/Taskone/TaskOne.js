import React from 'react'
import NavBar from './NabBar'

export default function TaskOne({history}) {
    return (
        <div className="tsk1">
            <NavBar history={history} />
        </div>
    )
}
